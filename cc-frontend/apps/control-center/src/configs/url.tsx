// backend url to be given when application is running in develeopement mode
const url_backend = 'http://localhost:8000';

// uncomment this when running application using docker
// when using docker backend url is not required as the nginx reverse proxy
// will deal with redirection

// const url_backend=''

export default url_backend;
